# План практики по книге «PHP 8» (Котеров, Симдянов)

Цель — подготовка к разработке на **Bitrix24**. Упор на ООП, работу с данными, HTTP/формы, файлы и стандарты (PSR), которые в Битриксе встречаются постоянно.

## Как работать с этим планом

1. Прочитали главу → сразу открыли соответствующий раздел ниже.
2. Сделали **мини-задачи** (обычно 15–40 минут). Не пропускайте «Критерий готово» — это ваш чеклист.
3. Если глава есть в таблице вех — **доработайте большой проект** в тот же день или на следующий.
4. После главы: короткий коммит в Git (`глава N: …`) — привычка, полезная в реальной работе.
5. Застряли — приходите с кодом/ошибкой; разберём вместе.

### Структура папок (создайте один раз)

```
php-study/
├── project/                 # большой проект «Мини-CRM»
│   ├── public/              # точка входа (index.php и т.п.)
│   ├── src/                 # классы (появится позже)
│   ├── storage/             # JSON, загрузки, логи
│   └── templates/           # HTML-шаблоны (позже)
├── exercises/               # мини-задачи
│   ├── glava_01/
│   ├── glava_02/
│   └── ...
└── README.md
```

Имена файлов упражнений: `exercises/glava_XX/task_N.php` (например `glava_05/task_1.php`).

### Условные обозначения

| Метка | Значение |
|---|---|
| ⭐ | Обязательно для прогресса |
| 🔥 | Сильно пригодится в Битрикс24 |
| 💡 | Углубление / «на вырост» |
| ⏱ | Ориентировочное время |

---

## 🏗 Большой проект: «Мини-CRM / Органайзер задач»

**Суть:** список клиентов и задач (имя, email, статус, дедлайн, комментарий). Проект растёт от «портянки» до структуры, близкой к компонентам Битрикса.

### Вехи проекта

| После главы | Что добавляем | Критерий готово |
|---|---|---|
| 4–5 | `public/index.php` — HTML-таблица из 3 жёстко заданных задач | В браузере видна таблица с заголовками и 3 строками |
| 9–10 | Цвета статусов `new` / `in_progress` / `done`; цикл по массиву | Три разных цвета; нет копипасты строк вручную |
| 11 | Задачи = массив ассоциативных массивов; сортировка по дедлайну | Ближайший дедлайн сверху |
| 12–13 | Функции: рендер, подсчёт просроченных, фильтр по статусу | Логика не в одном длинном скрипте |
| 6, 14 | Класс `Task`: `isOverdue()`, `markDone()` | Работа идёт через объекты, не через «голые» массивы |
| 15 | `getTasks()` — генератор | `foreach` по генератору работает; большие списки не держатся целиком в памяти (хотя бы по идее) |
| 16–17 | `htmlspecialchars`, читаемые даты, обрезка описания | XSS через описание не «ломает» страницу |
| 18–19 | Форма добавления (POST) + прикрепление файла | После отправки задача появляется в списке; файл лежит в `storage/uploads/` |
| 20 | Фильтр в `$_COOKIE`, простая авторизация в `$_SESSION` | После перезагрузки браузера фильтр помнится; без логина форма недоступна |
| 21 | Валидация email и даты через `filter_var` | Невалидные данные не сохраняются, показывается ошибка |
| 23–25 | Хранение в `storage/tasks.json`, бэкап в `storage/backup/` | Перезапуск сервера не стирает задачи |
| 27 | `DateTime` для дедлайнов; «осталось N дней» | В таблице виден человекочитаемый остаток |
| 28 | Regex: телефон клиента; поиск по описанию | Невалидный телефон отклоняется; поиск находит совпадения |
| 30–34 | Наследование, интерфейсы, трейты, исключения | Есть `UrgentTask`, `Notifiable`, `HasComments`, `TaskNotFoundException` |
| 36 | Namespaces: `App\Model`, `App\Repository`, `App\Controller` | Автозагрузка без `require` каждого файла вручную |
| 38–39 | `TaskCollection` + Reflection для отладки | `foreach` по коллекции; страница `/debug` показывает свойства `Task` |
| 41–42 | PostgreSQL + PDO CRUD | JSON можно отключить; CRUD через подготовленные запросы |
| 44 | CURL: webhook/Telegram при просрочке | При срабатывании уходит HTTP-запрос (хотя бы на webhook.site) |
| 45 | Redis-кеш списка задач | Повторный запрос быстрее / не ходит в БД без нужды |
| 46–48 | Composer, PSR-4, PSR-12 | `composer dump-autoload` поднимает классы; код отформатирован |
| 50 | «Компонент» по аналогии с Bitrix | Входные параметры → логика → шаблон результата |

К концу путь такой же, как у типичного модуля/компонента в экосистеме Битрикса: данные → слой доступа → бизнес-логика → шаблон.

---

## ✏️ Мини-задачи по главам

---

### Часть I. Основы веб-разработки

#### Гл. 1–2. Интернет и HTTP
🔥 ⏱ 30–45 мин

**Зачем для Битрикса:** REST API Битрикс24, вебхуки, AJAX-компоненты — всё это HTTP. Нужно уверенно читать заголовки, коды ответа, методы GET/POST.

**Задача 1 ⭐ — Разбор реального ответа**
1. В терминале: `curl -v https://example.com` (или любой публичный сайт).
2. В файле `exercises/glava_01/headers.md` выпишите:
   - строку статуса (код + причина);
   - 5–7 заголовков ответа и одним предложением — зачем каждый;
   - чем GET отличается от POST на бытовом уровне.
3. Отдельно отметьте, что такое `Content-Type` и зачем он нужен PHP-скрипту.

**Критерий готово:** в файле есть код ответа и осмысленные пояснения, не копипаста «как есть».

**Задача 2 ⭐ — Мини-клиент на PHP**
1. Скрипт `exercises/glava_02/fetch_meta.php`.
2. Через `file_get_contents` с `stream_context_create` **или** расширение CURL сделайте GET на выбранный URL.
3. Выведите только: HTTP-код и `Content-Type` (остальное тело не печатайте целиком).

**Критерий готово:** запуск из CLI (`php fetch_meta.php`) показывает две строки с кодом и типом.

**Задача 3 💡**
Сравните ответ `curl -I` (только заголовки) и обычный GET. Когда хватает HEAD?

---

#### Гл. 3. Установка PHP
⭐ ⏱ 20–40 мин

**Зачем:** локальная среда — основа любой практики и будущего Docker/окружения Битрикса.

**Задача 1 ⭐**
1. Файл `exercises/glava_03/info.php` с `phpinfo();`.
2. Запуск: `php -S localhost:8000 -t exercises/glava_03`.
3. В браузере откройте `/info.php`, найдите версию PHP, `Loaded Configuration File`, включён ли `pdo_pgsql` / `curl` / `mbstring`.

**Критерий готово:** вы знаете свою версию PHP и путь к `php.ini`; сервер отдаёт страницу без ошибок.

**Задача 2 🔥 / 💡**
Если ещё нет Docker — поднимите контейнер с PHP 8.x (официальный образ). Зафиксируйте команду запуска в `exercises/glava_03/docker-notes.md`. Для Битрикс24 позже пригодится похожий стек (PHP + веб-сервер + БД).

---

### Часть II. Основы языка PHP

#### Гл. 4. Характеристика языка
⭐ ⏱ 15–20 мин

**Задача 1 ⭐**
1. `exercises/glava_04/syntax_error.php` — намеренно уберите `;` или закроющую скобку.
2. Запустите, скопируйте текст ошибки в комментарий внизу файла (после исправления или во втором файле `error_log.txt`).
3. Исправьте и убедитесь, что скрипт выполняется.

**Критерий готово:** вы понимаете, *где* PHP указывает на ошибку (файл, строка) и умеете читать сообщение.

**Задача 2 💡**
Включите отображение ошибок (`display_errors`) только для учебного окружения; в комментарии напишите, почему на проде так делать нельзя.

---

#### Гл. 5. Переменные и типы
⭐ 🔥 ⏱ 35–50 мин

**Зачем для Битрикса:** данные из `$_REQUEST`, ORM, REST приходят «как строки»; без понимания типов легко словить тонкие баги.

**Задача 1 ⭐ — Инспектор переменной**
```
exercises/glava_05/task_1_inspect.php
```
Функция `inspect(mixed $value): void` печатает тип (`gettype` / `get_debug_type`) и `var_dump($value)`.
Проверьте на: `null`, `true`, `42`, `3.14`, `"42"`, `[1,2]`, объект `stdClass`.

**Критерий готово:** для каждого примера в выводе виден тип и значение.

**Задача 2 ⭐ — Приведение типов**
Файл `task_2_coercion.php`. Для каждой пары выведите выражение, результат и краткий комментарий «почему так»:
1. `"5" + 3`
2. `"5" . 3`
3. `"abc" == 0` (и при возможности сравните поведение/предупреждения PHP 8)
4. `"" == false` vs `"" === false`
5. `(int)"12abc"` и `(int)"abc"`

**Критерий готово:** 5 примеров с пояснениями; вы сознательно используете `===` там, где важна строгость.

**Задача 3 💡**
Объявите типизированные переменные / параметры (`int`, `string`, `?string`) и вызовите с неверным типом — посмотрите `TypeError`.

**Веха проекта:** после гл. 4–5 сделайте первую таблицу задач в `project/public/index.php`.

---

#### Гл. 6. Классы и объекты
⭐ 🔥 ⏱ 40–60 мин

**Зачем:** почти весь современный Битрикс и D7 — ООП.

**Задача 1 ⭐ — Класс Point**
`exercises/glava_06/Point.php` + `demo.php`:
- свойства `float $x`, `float $y`;
- метод `distanceTo(Point $other): float` (формула расстояния);
- создайте две точки, выведите расстояние.

**Задача 2 ⭐ — clone vs присваивание**
Покажите:
1. `$b = $a;` — оба ссылаются на один объект; изменение `$b->x` меняет `$a`.
2. `$c = clone $a;` — независимая копия.

**Критерий готово:** в выводе явно видно различие до/после изменения координат.

**Задача 3 💡**
Метод `__toString(): string`, возвращающий `"(x; y)"`.

---

#### Гл. 7. Константы
⭐ ⏱ 20–30 мин

**Задача 1 ⭐**
Класс `TaskStatus` (или константы внутри будущего `Task`):
- `STATUS_NEW`, `STATUS_IN_PROGRESS`, `STATUS_DONE`;
- функция/метод `label(string $status): string` возвращает русскую подпись;
- в демо нигде нет «магических» строк `'new'` вне констант.

**Критерий готово:** смена значения константы в одном месте меняет поведение везде.

**Задача 2 💡**
`define` vs `const` — два коротких примера и когда что уместно.

---

#### Гл. 8. Операторы
⭐ ⏱ 25–35 мин

**Задача 1 ⭐ — Приоритет**
Составьте выражение с 4–5 операторами (`+`, `.`, `??`, `?:`, сравнением и т.д.).
Сначала **запишите ожидаемый результат** в комментарии, затем выполните и сверьте.

**Задача 2 ⭐ — == vs ===**
Пять пар: `(0, "0")`, `(false, "")`, `(null, false)`, `([], false)`, `(1, true)` — для каждой оба сравнения.

**Критерий готово:** таблица/вывод «пара | == | ===» и понимание, почему в условиях Битрикса чаще `===`.

---

#### Гл. 9. Условия
⭐ 🔥 ⏱ 25–40 мин

**Задача 1 ⭐**
Цепочка `if / elseif` по статусу задачи → цвет CSS. Перепишите через `match` (PHP 8).
Добавьте ветку `default`.

**Задача 2 💡**
Тернарный оператор vs `match` — когда что читаемее (краткий комментарий в файле).

**Веха проекта:** цвета статусов + цикл (вместе с гл. 10).

---

#### Гл. 10. Циклы
⭐ ⏱ 30–45 мин

**Задача 1 ⭐ — Простые числа**
Найдите все простые числа до 100. Используйте вложенные циклы и хотя бы раз `continue` или `break`.

**Задача 2 ⭐**
`foreach` по списку задач: пропустите (`continue`) уже `done`, для остальных выведите название.

**Критерий готово:** оба скрипта дают ожидаемый список без лишних элементов.

---

#### Гл. 11. Ассоциативные массивы
⭐ 🔥 ⏱ 40–55 мин

**Зачем:** выборки, `$_POST`, JSON из REST Битрикс24 — сплошные ассоциативные массивы.

**Задача 1 ⭐ — Студенты**
Массив студентов: `name`, `grade`.
1. Средний балл.
2. Сортировка по убыванию оценки (`usort` / `uasort`).
3. Группировка «сдал» (≥ 3) / «не сдал».

**Критерий готово:** три блока вывода; сортировка стабильно верная на ваших данных.

**Задача 2 🔥**
Дан «ответ REST» как массив — достаньте 2–3 вложенных поля через ключи (`['result']['tasks'][0]['title']` и т.п.). Смоделируйте структуру сами.

**Веха проекта:** задачи как assoc-массивы + сортировка по дедлайну.

---

#### Гл. 12. Функции
⭐ 🔥 ⏱ 35–50 мин

**Задача 1 ⭐**
Одна функция с:
- параметром по умолчанию;
- variadic (`...$ids`);
- вызовом через именованные аргументы.

**Задача 2 ⭐**
Типы параметров и возврата: `function sum(int $a, int $b): int`. Намеренно передайте строку — поймайте/уберите `TypeError`.

**Критерий готово:** все три стиля аргументов продемонстрированы в одном или соседних файлах.

**Веха проекта:** вынести рендер / фильтр / подсчёт просроченных в функции.

---

#### Гл. 13. Сложные функции
⭐ ⏱ 40–60 мин

**Задача 1 ⭐**
Рекурсия: факториал и числа Фибоначчи (с ограничением глубины / для малых n).

**Задача 2 ⭐**
Свой упрощённый `array_map`: принимает callable и массив, возвращает новый массив. Внутри — анонимная функция или стрелочная; покажите замыкание (use / захват переменной).

**Критерий готово:** `my_map` удваивает числа `[1,2,3]` → `[2,4,6]`.

---

#### Гл. 14. Методы класса
⭐ 🔥 ⏱ 40–55 мин

**Задача 1 ⭐**
Класс с конструктором (обязательные + опциональные параметры), приватными свойствами, геттерами/сеттерами. В сеттере статуса — проверка допустимых значений.

**Задача 2 💡**
Readonly-свойства (PHP 8.1+, если версия позволяет) или имитация «задать только в конструкторе».

**Веха проекта:** класс `Task` с `isOverdue()`, `markDone()`.

---

#### Гл. 15. Генераторы
⭐ ⏱ 30–45 мин

**Задача 1 ⭐**
Генератор бесконечного Фибоначчи; в цикле возьмите первые 10 значений и сделайте `break`.

**Задача 2 💡**
Генератор, читающий файл построчно через `yield` (подготовка к большим логам).

**Веха проекта:** `getTasks()` как генератор.

---

### Часть III. Обработка текста и HTML

#### Гл. 16. Строковые функции
⭐ 🔥 ⏱ 30–45 мин

**Зачем:** шаблоны, почта, превью текста в списках CRM.

**Задача 1 ⭐**
Функция `truncate(string $text, int $limit): string` — обрезает до N символов с `…`, **не разрывая слово** (обрезать по последнему пробелу до лимита). Учтите UTF-8 (`mb_*`).

**Критерий готово:** на фразе длиннее лимита многоточие есть; слово на границе не разрезано пополам.

**Задача 2 ⭐**
Поиск/замена, разбиение строки в массив тегов, сборка обратно через `implode`.

---

#### Гл. 17. HTML и PHP
⭐ 🔥 ⏱ 30–45 мин

**Задача 1 ⭐**
Страница-профиль: имя, email, «о себе» из переменных. Все выводы через `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')`.
В поле «о себе» намеренно положите `<script>alert(1)</script>` — в браузере должен быть текст, а не скрипт.

**Критерий готово:** XSS-пример нейтрализован; вёрстка валидна на глаз.

**Веха проекта:** экранирование + обрезка описаний + даты.

---

#### Гл. 18. Формы
⭐ 🔥 ⏱ 45–70 мин

**Зачем:** пользовательский ввод в админках, публичных формах, обработчиках.

**Задача 1 ⭐**
Форма: text, checkbox, `<select>`, textarea. Метод POST на тот же или отдельный скрипт.
После отправки выведите все значения аккуратно (с экранированием).
Пустые обязательные поля — сообщение об ошибке, форма с сохранением введённого.

**Критерий готово:** полный цикл «показать → отправить → показать результат/ошибки».

**Задача 2 🔥**
Различие `$_GET` и `$_POST`: одна ссылка с query-параметром и одна форма POST — сравните, что видно в URL.

**Веха проекта:** форма добавления задачи.

---

#### Гл. 19. Загрузка файлов
⭐ 🔥 ⏱ 45–70 мин

**Задача 1 ⭐**
Форма `enctype="multipart/form-data"`, поле `file`.
Проверки:
- ошибка загрузки (`$_FILES['...']['error']`);
- MIME/расширение (изображение);
- максимальный размер;
- сохранение в `storage/uploads/` с уникальным именем (не доверять имени пользователя).

**Критерий готово:** валидный файл сохраняется; `.php` или слишком большой файл — отклоняются.

**Веха проекта:** вложение к задаче.

---

#### Гл. 20. Суперглобальные массивы
⭐ 🔥 ⏱ 40–60 мин

**Задача 1 ⭐**
Счётчик визитов через `$_SESSION` (не забыть `session_start()`).

**Задача 2 ⭐**
«Тёмная тема»: значение в `$_COOKIE` на 7 дней; переключатель ссылкой/формой.

**Задача 3 🔥**
Мини-логин: логин/пароль в сессии (пароль пока можно захардкодить для учёбы; позже — `password_hash`). Без сессии — редирект на login.

**Критерий готово:** сессия переживает обновление страницы; cookie видна в DevTools; «выход» чистит сессию.

**Веха проекта:** фильтр в cookie + авторизация в session.

---

#### Гл. 21. Фильтрация и валидация
⭐ 🔥 ⏱ 35–50 мин

**Задача 1 ⭐**
`filter_var` / `filter_input` для email, URL, целого числа, булева флага. Для каждого — валидный и невалидный пример.

**Задача 2 ⭐**
Свой набор правил для «карточки клиента»: email обязателен и валиден; возраст — int в диапазоне; сайт — URL или пусто.

**Критерий готово:** невалидные данные не проходят; сообщения об ошибках понятны.

**Веха проекта:** валидация email и дедлайна при сохранении задачи.

---

### Часть IV. Стандартные функции PHP

#### Гл. 22. Математика
⭐ ⏱ 20–30 мин

**Задача 1 ⭐**
CLI/веб-калькулятор: `+ - * /`, степень, округление (`round`, `ceil`, `floor`), случайное число в диапазоне. Деление на ноль — обработка.

**Критерий готово:** пять операций проверены руками; деление на 0 не роняет скрипт «молча».

---

#### Гл. 23. Файлы
⭐ 🔥 ⏱ 35–50 мин

**Задача 1 ⭐**
Большой текстовый файл (или сгенерируйте) — построчное чтение (`fgets` / `SplFileObject`), подсчёт слов и строк. Не загружайте весь файл через `file_get_contents`, если учите потоковое чтение.

**Задача 2 ⭐**
Запись лога: `file_put_contents(..., FILE_APPEND | LOCK_EX)`.

**Веха проекта (23–25):** `tasks.json` + бэкапы.

---

#### Гл. 24. Каталоги
⭐ ⏱ 30–45 мин

**Задача 1 ⭐**
Рекурсивный обход каталога: дерево с отступами (имя файла/папки). Исключите `.` и `..`.

**Задача 2 💡**
Подсчёт суммарного размера файлов в каталоге.

---

#### Гл. 25. Права доступа
⭐ 🔥 ⏱ 20–35 мин

**Задача 1 ⭐**
Перед сохранением JSON проверьте `is_writable` для файла или каталога. Если нет — понятная ошибка в лог/на экран (в учебном режиме).

**Критерий готово:** скрипт не «падает» с Warning без вашей обработки, когда запись запрещена (смоделируйте права или несуществующий путь).

---

#### Гл. 26. Внешние программы
⭐ ⏱ 20–30 мин

**Задача 1 ⭐**
`exec` / `shell_exec` / `proc_open` — вызов `dir` (Windows) или `ls` (Linux), разбор вывода в массив строк.
⚠️ Не передавайте в shell сырой пользовательский ввод.

**Критерий готово:** вывод команд разобран; в комментарии — предупреждение про инъекции в shell.

---

#### Гл. 27. Дата и время
⭐ 🔥 ⏱ 35–50 мин

**Задача 1 ⭐**
Сколько полных дней до ближайшего дня рождения (`DateTime` / `DateTimeImmutable`, `diff`).

**Задача 2 ⭐**
Форматирование: `d.m.Y H:i`, ISO-8601; часовой пояс `Europe/Moscow` (или ваш).

**Критерий готово:** для даты в прошлом/будущем ответ корректный; код не сравнивает даты как строки «на глаз».

**Веха проекта:** дедлайны на `DateTime`, колонка «осталось N дней».

---

#### Гл. 28. Регулярные выражения
⭐ 🔥 ⏱ 40–55 мин

**Задача 1 ⭐**
Валидатор телефона (задайте понятный формат, например `+7` и 10 цифр, или гибкий E.164-упрощённый).

**Задача 2 ⭐**
Из текста извлечь все email (`preg_match_all`).

**Задача 3 💡**
Подсветка совпадений поискового слова в описании задачи (с экранированием HTML!).

**Веха проекта:** телефон + поиск по описанию.

---

#### Гл. 29. Разные функции
⭐ 🔥 ⏱ 30–45 мин

**Задача 1 ⭐**
Регистрация/проверка пароля: `password_hash` + `password_verify`. Никакого хранения пароля открытым текстом.

**Задача 2 💡**
`serialize` / `json_encode` — когда что уместнее для вашего `tasks.json` (обычно JSON).

---

### Часть V. ООП

#### Гл. 30. Наследование
⭐ 🔥 ⏱ 40–60 мин

**Задача 1 ⭐**
`Animal` → `Dog`, `Cat`; переопределение `makeSound()`. Покажите вызов родительского метода через `parent::`, если уместно.

**Задача 2 🔥 (проект)**
Абстрактный `Task`, наследник `UrgentTask` (например, иной приоритет или расчёт просрочки).

**Критерий готово:** полиморфизм: массив разных животных/задач обрабатывается единообразно.

---

#### Гл. 31. Интерфейсы
⭐ 🔥 ⏱ 35–50 мин

**Задача 1 ⭐**
`interface Shape { public function area(): float; }` — `Circle`, `Square`.

**Задача 2 🔥**
`interface Notifiable { public function notify(string $message): void; }` — реализация для задачи или пользователя.

**Критерий готово:** функция принимает `Shape`/`Notifiable` и работает с любой реализацией.

---

#### Гл. 32. Трейты
⭐ ⏱ 25–40 мин

**Задача 1 ⭐**
Трейт `Loggable` с `log(string $message): void` (запись в файл). Подключите к двум разным классам.

**Задача 2 🔥 (проект)**
Трейт `HasComments` — добавление/получение комментариев к задаче.

**Критерий готово:** оба класса пишут в лог без копипасты метода.

---

#### Гл. 33. Перечисления (enum)
⭐ 🔥 ⏱ 25–40 мин

**Задача 1 ⭐**
```php
enum Status: string {
    case New = 'new';
    case InProgress = 'in_progress';
    case Done = 'done';
}
```
Метод `label(): string`. Замените строковые константы статусов на enum в учебном примере (и по возможности в проекте).

**Критерий готово:** `Status::from('new')` / `tryFrom` для невалидного значения обработаны.

---

#### Гл. 34. Исключения
⭐ 🔥 ⏱ 40–55 мин

**Задача 1 ⭐**
Класс `Wallet`, метод `withdraw`. При нехватке средств — `InsufficientFundsException`. Снаружи — `try/catch`, понятное сообщение.

**Задача 2 🔥 (проект)**
`TaskNotFoundException` при поиске задачи по id.

**Критерий готово:** нормальный путь без исключений; ошибочный — только через exception, не через `false` + тихий провал.

---

#### Гл. 35. Обработка ошибок
⭐ ⏱ 30–45 мин

**Задача 1 ⭐**
`set_error_handler` — пишите ошибки в `storage/php-errors.log` с датой и файлом/строкой.
Спровоцируйте warning (например, обращение к неопределённой переменной в контролируемом примере) и убедитесь, что запись появилась.

**Критерий готово:** лог-файл создаётся и пополняется; в комментарии — отличие Error/Exception в PHP 8.

---

#### Гл. 36. Пространства имён
⭐ 🔥 ⏱ 40–60 мин

**Зачем:** структура модулей Битрикс (`Namespace\ClassName`), автозагрузка.

**Задача 1 ⭐**
Три класса в разных namespace (`App\Model`, `App\Service`, …). `spl_autoload_register` или уже простая PSR-4-подобная загрузка по пути файла.

**Критерий готово:** точка входа подключает только автозагрузчик; классы находятся сами.

**Веха проекта:** разнести модель / репозиторий / контроллер по `App\...`.

---

#### Гл. 37. Паттерны проектирования
⭐ 🔥 ⏱ 50–80 мин

**Задача 1 ⭐**
Singleton для «подключения к БД» (учебный, без реального сервера можно с заглушкой). Объясните в комментарии, почему в больших приложениях и тестах Singleton часто избегают.

**Задача 2 ⭐**
Factory Method: фабрика создаёт `Task` или `UrgentTask` по типу из формы/JSON.

**Задача 3 💡**
Кратко сравните с тем, как в Битриксе встречаются Service Locator / фабрики классов (на уровне идеи, без глубокого Битрикс API).

---

#### Гл. 38. Итераторы
⭐ ⏱ 40–55 мин

**Задача 1 ⭐**
Класс-коллекция `TaskCollection`: `Iterator` (или `IteratorAggregate`). `foreach ($collection as $task)` работает.

**Критерий готово:** реализованы нужные методы итератора; демо выводит все элементы.

**Веха проекта:** коллекция задач в проекте.

---

#### Гл. 39. Отражения (Reflection)
⭐ ⏱ 30–45 мин

**Задача 1 ⭐**
Через `ReflectionClass` выведите для `Task`: свойства, методы, есть ли атрибуты (если уже изучали).
Сделайте учебную страницу/CLI `debug_task.php`.

**Критерий готово:** список публичных методов совпадает с реальным классом.

---

### Часть VI. Расширения PHP

#### Гл. 41–42. PostgreSQL и PDO
⭐ 🔥 ⏱ 1.5–3 ч (можно разбить на 2 дня)

**Зачем:** сущности Битрикса живут в БД; PDO учит безопасным запросам (SQL injection).

**Задача 1 ⭐**
Таблица `books` (id, title, author, year). Через PDO:
- connect;
- INSERT / SELECT / UPDATE / DELETE;
- **только** подготовленные запросы (`prepare` + `execute`).

**Задача 2 ⭐**
Поймайте `PDOException`, выведите понятный лог (без пароля БД в выводе).

**Критерий готово:** полный CRUD руками проверен; строка с кавычками в title не ломает SQL.

**Веха проекта:** перенос задач из JSON в PostgreSQL.

---

#### Гл. 43. Изображения (GD)
⭐ ⏱ 40–60 мин

**Задача 1 ⭐**
Сгенерировать изображение: фон, текст (watermark или простая капча), сохранить PNG/JPEG в `storage/`.

**Критерий готово:** файл открывается как картинка; текст читаем.

---

#### Гл. 44. Сети и CURL
⭐ 🔥 ⏱ 40–60 мин

**Задача 1 ⭐**
GET к публичному JSON API (курсы валют, IP, и т.п.), разбор `json_decode`, аккуратный вывод.

**Задача 2 🔥**
POST на https://webhook.site (или аналог) — имитация уведомления.

**Критерий готово:** обработка HTTP-ошибок и невалидного JSON; таймаут CURL задан.

**Веха проекта:** уведомление при просроченной задаче.

---

#### Гл. 45. Redis
⭐ 💡 ⏱ 40–60 мин

**Задача 1 ⭐**
Закешировать «тяжёлый» результат (можно `sleep(2)` как имитацию) в Redis с TTL 30–60 сек. Второй запрос должен быть быстрым, пока TTL жив.

**Критерий готово:** видно различие «холодный / тёплый» кеш (время или явный флаг в выводе).

**Веха проекта:** кеш списка задач.

---

### Часть VII. Компоненты и культура кода

#### Гл. 46. Composer
⭐ 🔥 ⏱ 40–60 мин

**Задача 1 ⭐**
`composer init`, зависимость `nesbot/carbon` (или аналог). Выведите «человеческую» дату через Carbon.
PSR-4 в `composer.json` для вашего `App\`.

**Критерий готово:** `vendor/` в `.gitignore`; `composer install` поднимает проект на чистой машине.

---

#### Гл. 47. PSR
⭐ 🔥 ⏱ 30–50 мин

**Задача 1 ⭐**
Прогоните код проекта через `php-cs-fixer` или `phpcbf` к PSR-12.
Исправьте автозагрузку под PSR-4, если ещё не сделано.

**Критерий готово:** стиль единообразен; namespaces совпадают с путями.

---

#### Гл. 48. Документирование
⭐ ⏱ 25–40 мин

**Задача 1 ⭐**
PHPDoc для `Task` и 2–3 публичных методов: `@param`, `@return`, `@throws`.
Краткий `README.md` проекта: как запустить, где конфиг БД.

**Критерий готово:** IDE/вы сами по докблокам понимаете контракт метода без чтения тела.

---

#### Гл. 49. Атрибуты
⭐ 💡 ⏱ 30–45 мин

**Задача 1 ⭐**
Свой атрибут `#[Route('/tasks')]` на классе или методе; чтение через Reflection.
Выведите найденный путь в CLI.

**Критерий готово:** атрибут реально читается из кода, не захардкожен второй раз в выводе.

---

#### Гл. 50. Свой компонент
⭐ 🔥 ⏱ 2–4 ч (финал)

Соберите «компонент» по аналогии с Битрикс-компонентом:

1. **Входные параметры** — например: `status`, `limit`, `show_overdue_only`.
2. **Контроллер/класс компонента** — достаёт данные из репозитория, готовит `$result`.
3. **Шаблон** — только отображение `$result` (минимум логики).
4. **Подключение** одной строкой/вызовом из `index.php`, как «вставка компонента на страницу».

**Критерий готово:**
- смена шаблона не ломает выборку данных;
- параметры влияют на результат;
- структуру можно объяснить фразой: «как bitrix:component, только учебный».

Это финальная веха большого проекта.

---

## Чеклист привычек (весь курс)

После каждых 2–3 глав отмечайте:

- [ ] Код в Git, осмысленные сообщения коммитов
- [ ] Секреты (пароли БД) не в репозитории — вынесены в конфиг/`getenv`
- [ ] Пользовательский вывод экранируется
- [ ] SQL только через prepared statements
- [ ] Ошибки логируются, а не только `echo` на прод-мышлении
- [ ] Имена классов/методов понятны без комментария «что это»

---

## Мост к Bitrix24 (после книги или параллельно с частями V–VII)

Когда базовый PHP уже не пугает, имеет смысл отдельно (не вместо книги):

1. Документация D7: ORM, события, модули.
2. Структура модуля (`install/`, `lib/`, namespaces).
3. Компоненты: `.parameters.php`, `.description.php`, `class.php`, `templates/`.
4. REST/входящие вебхуки — закрепление HTTP + JSON + CURL из глав 1–2 и 44.
5. Переписать кусок мини-CRM как учебный модуль/компонент на локальном Битрикс24 (когда будете готовы).

До этого момента **этот файл — ваш основной тренажёр**: глава → мини-задачи → веха проекта.

---

## Как просить помощь

Пишите так — так быстрее помочь:

1. Номер главы и файл (`exercises/glava_12/task_1.php`).
2. Что ожидали и что получили (текст ошибки / скрин / вывод).
3. Кусок кода или весь файл, если он короткий.

Удачи. Читаете главу — сразу практикуйте; практика важнее идеального конспекта.
